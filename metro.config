const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

config.watchFolders = []; // optionnel
config.resolver.blockList = [
  /android\/app\/\.cxx\/.*/,  
];

module.exports = config;
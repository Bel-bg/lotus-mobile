// ============================================
// LOTUS BUSINESS — Configuration (Firebase retiré)
// ============================================

// Les exports sont conservés vides pour ne pas casser les imports existants
export const app = {} as any;
export const auth = {} as any;
export const db = {} as any;
export const storage = {} as any;

// Collections Firestore (conservées pour référence si besoin)
export const COLLECTIONS = {
  LICENCES: 'licences',
  TENTATIVES: 'tentatives',
} as const;